# Base URL for the Astock backend. If unreachable, the UI automatically
# falls back to deterministic mock data so the frontend is fully demoable
# on its own.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
